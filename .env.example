# Copy to Netlify → Site settings → Environment variables (do NOT commit real values).
# Only the token is secret; the other IDs are hardcoded in netlify/functions/kommo-lead.js.

KOMMO_TOKEN=your-long-lived-kommo-token-here
