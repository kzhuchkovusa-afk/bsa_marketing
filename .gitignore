# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*~

# Logs
*.log

# Node (only if you later add tooling)
node_modules/
npm-debug.log*

# Local server / build artifacts
dist/
.cache/

# Secrets / env — never commit the Kommo token (set it in Netlify env vars)
.env
.env.*
!.env.example
.netlify/
